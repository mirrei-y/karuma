export global DateUtil {
    in property <[string]> weeks: ["月", "火", "水", "木", "金", "土", "日"];
}

export struct Date {
    month: int,
    day: int,
    dayOfWeek: int
}

export struct Time {
    hour: int,
    minute: int
}
